let wsPort = process.env.PORT;
let udpPort = process.env.PORT;
let statsPort = process.env.PORT;
const pino = require('pino');
const pretty = require('pino-pretty');
const stream = pretty({
  prettyPrint: { colorize: true }
});
const logger = pino(stream);

module.exports = {
  wsPort,
  udpPort,
  statsPort,
  logger
};
